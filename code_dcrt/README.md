# Knockoff inference package for Python

Based on *Candes et. al. (2016) Panning for Gold: Model-X Knockoffs for
High-dimensional Controlled Variable Selection*.

Currently works only on Python 3. To install run

```bash
pip install -r requirements.txt
pip install -e .  # or python setup.py build_ext --inplace
```
